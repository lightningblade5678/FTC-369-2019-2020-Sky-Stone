package org.firstinspires.ftc.teamcode;

        import com.qualcomm.robotcore.eventloop.opmode.Autonomous;
        import com.qualcomm.robotcore.eventloop.opmode.LinearOpMode;

        import org.firstinspires.ftc.teamcode.FinalBot.FinalBot;

@Autonomous(name="autonomousPink");
public class autonomousPink extends LinearOpMode {
    public FinalBot bot = new FinalBot(hardwareMap);
    @Override
    public void runOpMode()
    {
        bot.move (30.5,0.0);
        while (bot.pokesense (false){
        (bot.toneObserver if(black, bot.intake();
		else (bot.move (0.0, -1.0))}
        bot.move (-24.0, 0.0);
        bot.rotate (270.0);
        while bot.colorsense ≠≠ (blue, red,);{
        bot.move (1.0, 0.0)}


    }

}

